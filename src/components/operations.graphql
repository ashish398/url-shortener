query allURLs{

    queryURL{
        id
        long_url
        short_url
            
}
}

mutation addURL($url: AddURLInput!) {
  addURL(input: [$url]) {
    uRL {
        id
        short_url
        long_url
    }
  }
}